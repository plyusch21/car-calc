# Список задач — car-calc

Рабочий список. Отмечай `[x]`, когда проверено на телефоне.
Решения, стоящие за пунктами, — в памяти проекта «Calculator auto».

---

## Сейчас в работе

- [ ] **1. Закрыть открытые адреса.** `api/customs.js`, `api/parse-listing.js`, `api/share-relay.js` (только запись) не проверяют, кто пришёл. Посторонний может жечь платный ключ TKS и квоту Gemini, а занимая очередь `tks:ratelimit`, ронять расчёт. Добавить `authenticate(body.initData)`, клиенту — передавать `initData` в этих запросах.
- [ ] **2. Новый сброс формы.** После отправки или сохранения очищается всё, кроме объёма, мощности, типа двигателя, возраста, привода и трансмиссии. Перенесённые поля подсвечены как перенесённые.
- [ ] **3. Архив на 50 расчётов.** Во вкладке «Общие» истории — папка «Архив». Попадает туда всё автоматически, при переполнении старое стирается. Кнопка «Сохранить в историю» остаётся для постоянного хранения.
- [ ] **4. Автозаполнение перезаписывает всё.** Старое правило «только пустые поля» отменено — распознанное всегда побеждает.
- [ ] **5. Подсветка незаполненных полей** после распознавания.
- [ ] **6. Блок курса** над автозаполнением: дата сверху, ниже «● обновлён — 0,5730 — ЙЕНА / ₽». Зелёный — свежий, красный — проблема. Дату убрать с верха страницы. Корея — расчётный курс воны одной строкой.
- [ ] **7. Шапки карточек** по утверждённому макету: рабочие карточки — приглушённая (`#2f7dd1` 13%, текст `#e6edf1`, синяя линия снизу), чек — плотная (`#255aa1`, белый текст). По центру, заглавными, 12,5 px, межбуквенное 0,08 em.
- [ ] **8. Клавиатура сворачивается** при нажатии вне полей ввода и при прокрутке больше 20–30 px.
- [ ] **9. Убрать надпись** «автообновление 2 раза в сутки» — она неправдива.

---

## Дальше по коду

- [ ] **10. Таможня стирается** при изменении любого поля блока «Параметры авто», кроме марки/модели. При включённом ручном вводе не трогать.
- [ ] **11. Не показывать результат**, если ручной ввод таможни выключен, а автоматический расчёт не пришёл. Сейчас пустое поле считается за ноль и КП уходит заниженным.
- [ ] **12. Расширенное окно** для полей «Состояние» и «Примечания» — на пол-экрана, с переносом текста.
- [ ] **13. Запоминать** город доставки и тип кузова. Сам тумблер «Доставка по РФ» остаётся выключенным по умолчанию.
- [ ] **14. Распознавать цену** из объявления — добавить `price` и `priceCurrency` в схему `api/parse-listing.js`, подставлять только при совпадении валюты с маршрутом.
- [ ] **15. Считать возраст** из даты выпуска, с возможностью поправить.
- [ ] **16. Тумблеры:** убрать «Автозаполнение», убрать «Таможенная стоимость по каталогу» и «Полная пошлина для ЕАЭС» в раздел «Дополнительно». Остальные остаются.
- [ ] **17. Черновик формы** сохранять в `Telegram.WebApp.CloudStorage`, чтобы переживал переключение на WeChat и обратно.
- [ ] **18. Экранирование** значений в полях формы — сейчас кавычка в распознанном тексте обрезает поле.
- [ ] **19. Логотип** вынести из `index.html` в отдельный файл — сейчас 157 КБ из 296 КБ грузятся при каждом заходе.
- [ ] **20. Нативные элементы Telegram:** `MainButton` вместо кнопки «Рассчитать», `BackButton` для шитов, вибрация, `disableVerticalSwipes`.
- [ ] **21. Цвет шапки Telegram** в `index.html` — `#080d12` вместо `#121a1b`, как уже сделано в `deals.html`.

---

## Курсы: защита от молчаливой поломки

- [ ] **22. История курсов** за 3 месяца, отдельным хранилищем (не внутри блоба настроек). Смотреть в настройках.
- [ ] **23. Коридоры правдоподобия:** йена 0,3–0,9; юань 8–20; вона к USDT 1000–2000. Значение вне коридора не применяется.
- [ ] **24. Проверка на скачок:** разница с прошлым значением больше 10–15% — не применять молча.
- [ ] **25. Проверка на застой:** значение не менялось 3 дня — разбор сломан, сообщить.
- [ ] **26. Ежедневный сторож** (Vercel Cron): сверка с курсом ЦБ, сообщение в Telegram при расхождении.
- [ ] **27. Устойчивый разбор** ATB и Naver — искать по смыслу и коридору, а не по цепочке классов.
- [ ] **28. Курсы ЦБ справочно:** доллар, юань, йена, вона + евро — в подблок «Курсы ЦБ» в настройках. Не забыть номинал: йена за 100, вона за 1000.

---

## Данные и доступ

- [ ] **29. Разделить историю по пользователям.** Сейчас `state:history` — один блоб на всех, второй менеджер начнёт молча затирать чужое. Сделать ДО появления второго человека.
- [ ] **30. Защитить настройки** от затирания: проверка версии при сохранении.
- [ ] **31. Права на контрагентов** в `api/deals.js` — `saveParty`, `getParty`, `findPartyByPhone` и `bootstrap` не проверяют уровень доступа.
- [ ] **32. Правильные HTTP-коды** ошибок вместо 200 везде + `console.error` в catch, чтобы поломки были видны в панели Vercel.
- [ ] **33. Уведомления в Telegram:** новая заявка на доступ, одобрение доступа, сделка без движения неделю.

---

## Перед нативными приложениями

- [ ] **34. Расчёт на сервер** — `POST /v1/quotes/calculate` с полной разбивкой, использованными курсами и статусом таможни.
- [ ] **35. Справочники через API** — маршруты, комиссии, доставка, города.
- [ ] **36. Сессии и токены** вместо `initData` в каждом запросе.
- [ ] **37. КП на сервер** — текст и PNG рисует сервер, приложение получает готовый файл.
- [ ] **38. Расчёт как сущность** со снимком результата и курсов, а не строка истории.
- [ ] **39. Этапы сделки на сервер.**
- [ ] **40. PostgreSQL** вместо блобов в KV + объектное хранилище для фото и документов.

---

## Отдельной веткой

- [ ] **41. Отправка расчёта клиенту** — разбирается отдельно.

---

## Что трогать не надо

- Проверку подписи `initData` в `api/_lib/telegram.js` — сделана правильно.
- `mergeRatesState` / `mergeRoutesState` — лечат реальный класс ошибок.
- Пиннинг российского корневого CA в `api/rates.js` и `api/parse-listing.js`.
- Передачу стоимости в TKS в исходной валюте.
- Раздельное хранение сделок `deal:<id>` — не собирать в блоб.
- Отсутствие сборщика и фреймворка.
