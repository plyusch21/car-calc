# ТЗ 06. Убрать крон `deals-cleanup`

Модель: **Sonnet**. Одна сессия, один коммит с пушем.

Перед началом прочитать `.claude/map.md`. `index.html` и `deals.html` целиком не читать — только нужные диапазоны через `sed -n`, искать через `grep -n`. Номера строк в этом файле — по коду на коммите `fc8ab3b`; перед каждой правкой сверять `grep -n`. После правок, сдвигающих строки, — `bash tools/mapgen.sh`. Коммит и пуш в `main` — по правилам `CLAUDE.md`. Отступления от этого файла, согласованные по ходу, — записью в `РЕШЕНИЯ.md`.

**Решение владельца (В-10):** убрать — чистить ему нечего, а слот крона на Hobby-тарифе один из двух.

**Сделать:**
- `vercel.json`: удалить запись из `crons` (`:41`), из `routes` (`:31`) и из `builds` (`:19`).
- Удалить файл `api/deals-cleanup.js`.
- `CLAUDE.md:245`: фразу про `deals-cleanup.js daily cron` заменить на «Legacy `removed: true` rows from the old soft-delete are filtered out of `bootstrap`; nothing purges them — there were none left worth a cron slot».
- Фильтр `!d.removed` в `api/deals.js:272` **оставить** — он страхует от старых записей, если такие где-то есть, и ничего не стоит.
- `api/rates-refresh.js` — комментарий про крон не трогать, там свой.

**Проверить:** `vercel.json` валидный JSON (`node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"`); после деплоя в панели Vercel → Settings → Cron Jobs остался один крон `rates-refresh`; `/api/deals` работает как раньше.
